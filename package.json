{
  "name": "spotify-now-playing",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node --env-file-if-exists=.env --experimental-strip-types server.ts",
    "dev": "node --env-file-if-exists=.env --watch --experimental-strip-types server.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.10",
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.3",
    "prettier": "^3.8.1",
    "typescript": "^5"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^5.2.1"
  }
}
